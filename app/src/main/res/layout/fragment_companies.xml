<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.sduran.durandroid.fragments.CompaniesFragment">

    <TextView
        android:id="@+id/id_tv_companyNote"
        style="@style/NoteText"
        android:text="@string/detail_companies"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <android.support.v7.widget.RecyclerView
        android:layout_below="@id/id_tv_companyNote"
        android:id="@+id/id_rv_companies"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:dividerHeight="@dimen/empty_height"
        android:scrollbars="vertical"/>

</RelativeLayout>
